
name: Run Agent Manually

on:
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run your agent
        run: |
              agency agency copilot --agent get-unresolvedicmsv2
